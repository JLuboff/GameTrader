<!DOCTYPE html>
<html>
<head>
  <title>Trade Requests</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.7/flatly/bootstrap.min.css">
</head>
<body>
  <div class="container-fluid" style="margin-top: 50px;">
    <div class="row">
      <div class="col-md-2">
        <ul class="nav nav-pills nav-stacked">
          <li>Game Trader</li>
          <li><a href="/">Home</a></li>
          <li><a href="/findgames">Add Trades</a></li>
          <li><a href="/traderequests">Trade Requests
            {{#if tradeReqs}}
            <span id="tradeReqs" class="badge">{{tradeReqs}}</span>
            {{/if}}</a></li>
            <li>Account</li>
            {{#if loggedIn}}
            <li><a href="/logout">Logout</a></li>
            <li><a href="/profile">Profile</a></li>
            {{else}}
            <li><a href="/login">Login</a></li>
            {{/if}}
          </ul>
        </div>
      <div class="col-md-9">
  {{#if requestSent}}
    {{requestSent}}
    {{/if}}
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>Status</th>
          <th>Request</th>
          <th>Location</th>
          <th></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
    {{#each doc.tradeRequests}}
      <tr>
        <td>{{status}}</td>
        {{#if requestTo}}
        <td>You are requesting {{gameName}} for {{platform}}</td>
        <td> - </td>
        <td> - </td>
        <td><a href="/trade/cancel/{{gameId}}/{{requestTo.id}}" class="btn btn-primary btn-sm {{#if statusBoolean}}disabled{{/if}}">Cancel</a></td>
        {{/if}}
        {{#if requestFrom}}
        <td>{{requestFrom.username}} is requesting {{gameName}} for {{platform}}</td>
        <td>{{requestFrom.location}}</td>
        <td><a href="/viewTrades/{{requestFrom.id}}">{{requestFrom.username}}'s Games</a></td>
        <td><a href="/trade/accept/{{gameId}}/{{requestFrom.id}}" class="btn btn-primary btn-sm {{#if statusBoolean}}disabled{{/if}}">Accept</a> <a href="/trade/deny/{{gameId}}/{{requestFrom.id}}" class="btn btn-warning btn-sm {{#if statusBoolean}}disabled{{/if}}">Deny</a></td>
        {{/if}}
      </tr>
    {{/each}}
</div>
</div>
</div>
<script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.0/jquery.min.js'></script>
<script src="/js/bootstrap.min.js"></script>
  </body>
  </html>
